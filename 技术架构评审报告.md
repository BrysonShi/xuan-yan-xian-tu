# 技术架构评审报告

> **评审日期**：2026-08-17  
> **评审版本**：技术架构 v1.0  
> **评审专家**：前端架构师 / 游戏引擎开发者 / 数据工程师

---

## 一、问题统计

| 严重程度 | 前端架构师 | 游戏引擎开发者 | 数据工程师 | 合计 |
|----------|-----------|--------------|-----------|------|
| 🔴 致命 | 2 | 3 | 2 | **7** |
| 🟡 重要 | 3 | 3 | 3 | **9** |
| 🔵 建议 | 2 | 2 | 2 | **6** |
| **合计** | **7** | **8** | **7** | **22** |

---

## 二、专家1：前端架构师评审（Vue生态10年+经验）

### 🟢 值得肯定的设计

1. **数据驱动架构设计优秀**：engines/ 与 stores/ 分层清晰，引擎纯逻辑无Vue依赖，便于单测
2. **Pinia选型合理**：对于文字游戏的状态管理来说恰到好处，persistedstate插件减少手写序列化
3. **组件按功能域划分**：simulation/、reality/、character/ 的目录组织清晰
4. **composables抽取**：useTextAnimation、useAutoSave 等组合函数复用合理

---

### 🔴 致命问题

#### 【F-1】localStorage容量炸弹：存档数据无溢出防护

**问题描述**：  
架构使用 `pinia-plugin-persistedstate` + `lz-string` 将数据存入 localStorage。但 localStorage 的容量限制通常为 **5MB**（各浏览器略有差异）。当前设计中：
- 每个存档槽位包含完整的 Player 数据（含 `relationships`、`inventory`、`flags` 等可变长数据）
- 4个存档槽位 + `globalFlags` + `settings`
- 模拟历史记录、事件日志等持续膨胀的数组
- 随着游戏进行，单个存档可能达到 500KB-1MB

**潜在风险**：  
当 localStorage 写满后，`setItem` 会抛出 `QuotaExceededError`，导致自动存档静默失败。玩家可能在进行关键选择后发现存档未保存，关闭页面后进度全丢——这对文字游戏是**致命体验问题**。

**建议修改**：
1. 新增 `StorageMonitor` 模块，每次存档前检测剩余空间，低于 1MB 时触发清理策略
2. 实现分级存储：核心存档用 localStorage，非关键数据（事件日志全文、设置等）用 IndexedDB
3. 存档前捕获 `QuotaExceededError`，降级为仅保存核心数据
4. 在 UI 层增加存储使用量提示

**修改状态**：✅ 已更新至架构文档（新增 4.3 存储容量管理章节）

---

#### 【F-2】状态机 SETTINGS 回退状态丢失

**问题描述**：  
状态转换图中 `SETTINGS → previous_state`，但状态机实现中并未记录 `previous_state`。当从 `SIMULATION` 中打开设置再关闭时，无法正确回到模拟界面。

**潜在风险**：  
玩家在任何界面按设置再返回，可能跳转到错误状态（如从模拟界面跳到主菜单），导致模拟数据丢失。

**建议修改**：
1. 状态机增加 `previousState: GameState | null` 字段
2. 每次进入 SETTINGS 前自动记录当前状态
3. 增加 `RETURN_TO_PREVIOUS` 转换动作

**修改状态**：✅ 已更新至架构文档（状态机部分补充 previousState 机制）

---

### 🟡 重要问题

#### 【F-3】TypeScript 类型定义与文件扩展名矛盾

**问题描述**：  
架构文档中所有接口定义使用 TypeScript 语法（`interface`、`enum`），但项目目录中 engines/、stores/、models/ 下的文件扩展名全部为 `.js`。这意味着类型定义只存在于文档中，不会被编译器检查。

**建议修改**：  
- 方案A：将核心类型定义文件改为 `.ts`，至少 models/ 和 engines/ 使用 TypeScript
- 方案B：如果坚持用 `.js`，则需在每个文件顶部用 JSDoc `@typedef` 定义类型，并在 tsconfig.json 中开启 `checkJs`
- 建议采用方案A，至少对 engines/ 目录使用 `.ts`

**修改状态**：🟡 待讨论（记录在"仍需关注"清单）

---

#### 【F-4】EventBus 内存泄漏风险

**问题描述**：  
`EventBus` 使用 `Set<Function>` 存储 handler。Vue 组件在 `onMounted` 中注册事件监听，但如果忘记在 `onUnmounted` 中取消订阅，handler 会持续引用组件实例，阻止 GC 回收。

**潜在风险**：  
频繁的界面切换（如反复打开/关闭角色面板）会导致 handler 堆积，内存持续增长，最终在低端手机上卡顿崩溃。

**建议修改**：
1. 提供 `useEventBus` composable，内部在 `onUnmounted` 时自动清理所有注册
2. EventBus 增加 `weakRef` 模式或使用 `AbortController` 绑定组件生命周期
3. 开发模式下记录 handler 注册/注销日志，便于排查泄漏

**修改状态**：✅ 已更新至架构文档（EventBus 部分补充生命周期管理说明）

---

#### 【F-5】响应式数据粒度问题：Player 深层嵌套导致全量更新

**问题描述**：  
Player 数据有 5-6 层深度嵌套（如 `player.resources.spiritStones`），Pinia 的 `reactive()` 会对整个对象树建立响应式依赖。当模拟引擎批量更新属性时（一次事件可能改 5+ 个字段），会触发大量不必要的响应式更新。

**建议修改**：
1. 引擎层操作时使用 `toRaw()` 绕过响应式，批量修改后一次性触发更新
2. Store 中提供 `batchUpdate()` 方法，用 `shallowRef` 存储 Player 数据，手动触发更新
3. 或使用 `structuredClone` + 赋值方式替换整个 Player 对象

**修改状态**：✅ 已更新至架构文档（性能优化部分补充批量更新策略）

---

#### 【F-6】虚拟列表实现过于简单

**问题描述**：  
文档中的虚拟列表示例只处理了固定高度场景，但事件日志的每条记录高度不固定（有的事件文本长，有的短），直接按固定 `ITEM_HEIGHT` 计算会导致滚动位置错乱。

**建议修改**：  
改用动态高度虚拟列表方案，或限制每条事件日志的最大行数（CSS `line-clamp`），确保高度可预测。

**修改状态**：✅ 已更新至架构文档（虚拟列表部分修正为动态高度方案）

---

### 🔵 建议

#### 【F-7】缺少 Vue 组件错误边界

**建议**：增加全局 `onErrorCaptured` 或 `app.config.errorHandler`，捕获组件渲染错误后展示降级UI而非白屏，并自动触发紧急存档。

---

#### 【F-8】路由守卫未定义具体策略

**建议**：文档提到"路由守卫用于游戏状态拦截"，但未说明具体策略。建议补充：模拟进行中禁止路由跳转、离开页面前弹出确认、错误路由重定向等规则。

**修改状态**：✅ 已更新至架构文档（补充路由守卫策略说明）

---

## 三、专家2：游戏引擎开发者评审（8年+上线游戏经验）

### 🟢 值得肯定的设计

1. **事件调度器的权重系统**：加权随机 + 条件修正 + 气运影响，设计成熟
2. **存档多槽位策略**：4个槽位各有明确用途，预模拟存档设计周到
3. **ConditionEvaluator 独立引擎**：条件判断与业务解耦，扩展性好
4. **四选一奖励设计**：每类奖励各生成一个候选，保证了多样性

---

### 🔴 致命问题

#### 【G-1】无种子随机数：Bug 无法复现

**问题描述**：  
整个架构大量使用 `Math.random()`（事件选取、成功率判定、暴击判定、词条抽取等），但没有引入**种子随机数生成器（Seeded RNG）**。

**潜在风险**：
- 玩家报告"某次模拟中遇到诡异bug"，开发团队完全无法复现
- 同一个事件组合，每次运行结果不同，QA 无法进行回归测试
- 模拟器的"蝴蝶效应"依赖随机性，无法做 A/B 测试对比

**建议修改**：
1. 引入 `seedrandom` 库（或自实现 xoshiro128+ 算法）
2. 每次模拟启动时生成种子（可记录在存档中）
3. 模拟内所有随机操作使用 seeded RNG
4. 现实世界也用种子（以日期为种子，保证同一天事件一致）
5. 提供"种子回放"功能，测试时传入种子可精确复现

**修改状态**：✅ 已更新至架构文档（新增随机数生成器设计章节）

---

#### 【G-2】事件链执行中断无回滚机制

**问题描述**：  
`OptionResult` 的执行涉及多个子系统的数据变更（属性、资源、物品、关系、标记），但架构中没有事务/回滚机制。如果 `applyResult()` 执行到一半抛出异常（如数据引用错误），玩家数据会处于**不一致的中间状态**。

**潜在风险**：
- 灵石已扣但物品未发
- 属性已改但标记未设置，导致后续事件条件判断错误
- 存档时数据不一致，存档损坏

**建议修改**：
1. `applyResult()` 使用**快照-回滚模式**：执行前深拷贝当前状态，出错时回滚
2. 所有数据变更通过 `CalculationEngine.applyChanges()` 统一入口，保证原子性
3. 关键操作后插入一致性检查点

**修改状态**：✅ 已更新至架构文档（新增数据变更事务机制章节）

---

#### 【G-3】模拟过程浏览器崩溃的数据丢失

**问题描述**：  
模拟过程中（`SIMULATION` 状态），玩家可能在任意时刻切后台/关浏览器。当前设计只在"进入模拟前"和"关键选择后"存档，模拟**进行中**的实时状态（当前年、已历事件、角色临时状态）没有中间存档。

**潜在风险**：  
玩家进行了一次 10 分钟的模拟，在第 8 分钟遇到关键事件时切后台，浏览器被杀。重新打开后只能读取模拟前的存档，8 分钟进度全丢。

**建议修改**：
1. 模拟中每年推进后，将模拟状态缓存到 sessionStorage（不压缩，快速写入）
2. 或每 30 秒自动将模拟状态写入 localStorage 的临时区域
3. 恢复时检测是否有未完成的模拟，提示玩家继续

**修改状态**：✅ 已更新至架构文档（存档系统补充模拟中间存档机制）

---

### 🟡 重要问题

#### 【G-4】词条效果叠加优先级未定义

**问题描述**：  
多个词条可能同时影响同一个属性（如"身强体壮"加气血10%、"铜皮铁骨"加防御15%、组合效果再加成），但架构未定义：
- `flat_add` 和 `percent_add` 的计算顺序
- 同类效果的叠加方式（加法叠加 vs 乘法叠加）
- 组合效果是在单词条之后计算还是之前

**潜在风险**：  
数值策划无法准确预估最终数值，可能出现数值爆炸或某些 build 意外地过强/过弱。

**建议修改**：
1. 定义明确的计算管线：`基础值 → flat_add汇总 → percent_add汇总 → 触发类效果 → 组合效果`
2. 同类 percent_add 采用乘法叠加（避免加法叠加导致高数值时收益不合理）
3. 在 `CalculationEngine.collectTermBonus()` 中严格按管线执行

**修改状态**：✅ 已更新至架构文档（数值计算部分补充效果叠加管线）

---

#### 【G-5】浮点精度累积误差

**问题描述**：  
修为增长使用浮点数（`cultivation += dailyCultivation`），多次累加后精度漂移。例如 `10 * 0.1 * 0.1 * 0.1` 不等于 `0.001`。在模拟中累计几百年的修为增长，误差会显著影响突破时机。

**建议修改**：
1. 修为和灵石等关键数值使用整数存储（内部 ×100 或 ×1000）
2. 显示时再除以倍率格式化
3. 或使用 `Number.EPSILON` 比较而非 `===`

**修改状态**：✅ 已更新至架构文档（数值计算部分补充精度处理规范）

---

#### 【G-6】蝴蝶效应计算的复杂度不可控

**问题描述**：  
`processButterflyEffects()` 中 `butterflyLevel=3` 时"解锁全新剧情分支"，但未定义具体算法。如果蝴蝶效应基于模拟历史数据的差异计算，可能导致：
- 每次模拟后现实事件大规模变化，玩家无法形成稳定预期
- 事件变体数量指数增长，策划无法穷举所有变体
- 蝴蝶效应导致的异常状态无法被存档系统正确保存

**建议修改**：
1. Demo 版只实现 butterflyLevel 0-1（无偏移 + 微扰）
2. 微扰通过**事件变体表**实现：策划为关键事件预定义变体版本，而非动态生成
3. 蝴蝶效应等级提升只通过特定主线事件触发，而非自动累积

**修改状态**：✅ 已更新至架构文档（蝴蝶效应部分补充具体实现约束）

---

### 🔵 建议

#### 【G-7】缺少模拟的暂停/恢复机制

**建议**：`SimulationEngine` 应有 `pause()` / `resume()` 方法。当玩家切后台、打开设置、或接到系统中断时，模拟应暂停。当前设计只有 `status: 'running' | 'ended'`，建议增加 `'paused'` 状态。

**修改状态**：✅ 已更新至架构文档

---

#### 【G-8】战斗系统缺少速度/回合机制

**建议**：`calculateDamage()` 只计算单次伤害，但战斗可能是多回合的。需要定义：回合制还是自动结算？敏捷属性如何影响先手？战斗结果如何映射回事件系统？建议在 Demo 版简化为"自动战斗+文本叙述"，但预留扩展接口。

**修改状态**：🟡 待讨论（记录在"仍需关注"清单）

---

## 四、专家3：数据工程师评审（大厂数值策划背景）

### 🟢 值得肯定的设计

1. **数据与代码完全分离**：所有游戏内容以数据驱动，新增内容无需改代码
2. **存档版本号设计**：`schemaVersion` 字段为未来迁移预留了接口
3. **校验码机制**：`checksum` 防止简单篡改
4. **Slot 槽位与境界关联**：词条槽位随境界解锁，节奏合理

---

### 🔴 致命问题

#### 【D-1】资源无负值防护：灵石可被扣为负数

**问题描述**：  
架构中没有对 Resources 中的数值做**下限约束**。以下场景可能导致灵石变负：
- 事件结果中 `resourceChanges: { spiritStones: -20 }`，但玩家只有 15 灵石
- 模拟消耗灵石时未检查余额（理论上启动前应检查，但异常流程可能跳过）
- 交易购买物品时价格计算与扣除之间有竞态

**潜在风险**：  
负灵石不会立即崩溃，但会污染后续所有依赖灵石数值的条件判断（如 `spiritStones >= 50` 的事件条件在负值时永远不满足，玩家陷入死循环）。

**建议修改**：
1. 在 `applyResult()` 中所有数值变更后执行钳制：`Math.max(0, value)`
2. Resources 的每个字段定义 `min` 和 `max` 约束
3. 在数据模型中增加 `ResourceConstraints` 配置
4. 关键资源变更（灵石、修为）前后做断言检查

**修改状态**：✅ 已更新至架构文档（数值计算部分补充数值安全规范）

---

#### 【D-2】存档迁移机制只有框架无实现细节

**问题描述**：  
`schemaVersion` 存在但没有 `SaveMigrator` 的具体实现。当游戏更新后 Player 模型增加新字段（如正式版新增"恶名值"），老存档反序列化后新字段为 `undefined`，导致：
- 访问 `player.malice` 返回 `undefined`
- 数值运算 `player.malice * 0.5` 返回 `NaN`
- 整个游戏逻辑崩溃

**潜在风险**：  
这是所有长线运营游戏的**必考题**。Demo 版到正式版升级时，如果老玩家存档全部报废，会引发大规模差评。

**建议修改**：
1. 实现 `SaveMigrator` 类，包含逐版本迁移函数链：`migrate_v1_to_v2()`, `migrate_v2_to_v3()`...
2. 每次迁移对新字段填充合理默认值
3. 存档读取时自动检测 schemaVersion 并执行迁移链
4. 迁移前备份原始存档（防迁移代码 bug 损坏存档）

**修改状态**：✅ 已更新至架构文档（新增存档迁移实现细节章节）

---

### 🟡 重要问题

#### 【D-3】缺少关键数据接口定义

**问题描述**：  
存档模型中引用了 `Quest`、`MemoryFragment`、`Achievement`、`SimulationState`、`GameContext` 等接口，但文档中未给出定义。

**潜在风险**：  
开发者实现时可能遗漏关键字段，或各模块对同一概念的理解不一致。

**建议修改**：  
补充以下接口定义：
- `MemoryFragment`：含 id、type、relatedEventId、hintText、recommendedOption
- `Quest`：含 id、name、description、objectives、rewards、status
- `Achievement`：含 id、name、description、condition、reward、unlocked
- `GameContext`：含 player、currentScene、currentYear、completedEvents 等
- `SimulationState`：含 simId、startYear、currentYear、character、simTerms、eventLog 等

**修改状态**：✅ 已更新至架构文档（补充缺失接口定义）

---

#### 【D-4】词条效果冲突检测不完整

**问题描述**：  
词条系统定义了 4 组互斥关系（CFT-01~04），但检测逻辑只在"装备"时检查。以下边界情况未覆盖：
- 通过事件直接获得词条（不经过装备流程）是否需要检查冲突？
- 组合效果激活后，如果其中一个词条被移除，组合效果如何取消？
- 词条升级后可能与现有词条产生新的冲突

**建议修改**：
1. 统一通过 `equipTerm(slotIndex, termId)` 入口进行冲突检查
2. 事件获得词条时先进入"待装备"状态，由玩家手动装备
3. 组合效果检测在每次装备变更后重新执行

**修改状态**：✅ 已更新至架构文档（词条系统补充冲突检测边界说明）

---

#### 【D-5】存档压缩后解压失败无降级方案

**问题描述**：  
使用 `lz-string` 压缩存档，读取时解压。如果压缩数据损坏（如 localStorage 位翻转、浏览器 bug），`decompress` 返回 `null` 或抛出异常，整个存档不可读。

**建议修改**：
1. 解压失败时尝试读取 uncompressed 备份（存档时同时写一份未压缩的到另一个 key）
2. 或提示玩家导入备份
3. checksum 校验失败时同样触发降级逻辑

**修改状态**：✅ 已更新至架构文档（存档系统补充降级恢复方案）

---

#### 【D-6】新增词条类型的改动范围不明确

**问题描述**：  
如果要新增一个词条类型（如"血脉型"），需要修改哪些地方？当前文档没有明确的"新增检查清单"。

**建议修改**：  
提供"新增词条类型 Checklist"：
1. 在 `TermCategory` 类型中添加新类型
2. 在 `EffectType` 中添加对应的效果类型（如果需要）
3. 在 `CalculationEngine` 中添加新效果的处理逻辑
4. 在数据目录中新建数据文件
5. 在注册表中注册
6. 更新 UI 中的类型筛选/显示

**修改状态**：✅ 已更新至架构文档（扩展性设计部分补充新增检查清单）

---

### 🔵 建议

#### 【D-7】建议增加数据调试工具

**建议**：开发模式下提供 `window.debug` 命令集：
- `debug.player()` 查看当前玩家数据
- `debug.setAttr('luck', 100)` 直接修改属性
- `debug.event('EVT-T01')` 强制触发事件
- `debug.save()` / `debug.load()` 手动存取档

**修改状态**：🟡 待讨论（记录在"仍需关注"清单）

---

#### 【D-8】建议定义常量表的数据格式

**建议**：境界表、灵根表等常量数据建议统一使用配置对象而非硬编码。方便策划通过修改配置文件调整数值，而不需要改代码。

**修改状态**：✅ 已更新至架构文档（数据目录说明中补充配置规范）

---

## 五、已修改内容清单

以下修改已直接更新到技术架构文档中：

| 编号 | 修改位置 | 修改内容 | 来源 |
|------|---------|---------|------|
| 1 | 4.存档系统 | 新增 4.3 存储容量管理章节 | F-1 |
| 2 | 3.1状态机 | 补充 previousState 机制 | F-2 |
| 3 | 5.1 EventBus | 补充生命周期管理和自动清理说明 | F-4 |
| 4 | 7.性能优化 | 补充批量更新策略（shallowRef + toRaw） | F-5 |
| 5 | 7.2运行时优化 | 修正虚拟列表为动态高度方案 | F-6 |
| 6 | 6.文件结构 | 补充路由守卫策略说明 | F-8 |
| 7 | 3.游戏引擎 | 新增 3.6 随机数生成器（Seeded RNG）设计 | G-1 |
| 8 | 3.游戏引擎 | 新增 3.7 数据变更事务机制 | G-2 |
| 9 | 4.存档系统 | 补充模拟中间存档机制 | G-3 |
| 10 | 3.5数值计算 | 补充效果叠加计算管线 | G-4 |
| 11 | 3.5数值计算 | 补充浮点精度处理规范 | G-5 |
| 12 | 3.4现实引擎 | 补充蝴蝶效应具体实现约束 | G-6 |
| 13 | 3.3模拟引擎 | SimulationState 增加 paused 状态 | G-7 |
| 14 | 3.5数值计算 | 新增数值安全规范（钳制、约束、断言） | D-1 |
| 15 | 4.存档系统 | 新增存档迁移实现细节 | D-2 |
| 16 | 2.核心数据结构 | 补充缺失接口定义 | D-3 |
| 17 | 2.2词条系统 | 补充冲突检测边界说明 | D-4 |
| 18 | 4.存档系统 | 补充降级恢复方案 | D-5 |
| 19 | 8.扩展性设计 | 补充新增词条类型 Checklist | D-6 |
| 20 | 6.文件结构 | 补充常量数据配置规范 | D-8 |

---

## 六、仍需关注但暂不修改的问题

以下问题需要团队讨论后决定方案，暂不直接修改文档：

| 编号 | 问题 | 建议 | 优先级 |
|------|------|------|--------|
| F-3 | TypeScript 类型定义与 .js 文件扩展名矛盾 | 建议至少 engines/ 目录改为 .ts | P1 |
| G-8 | 战斗系统缺少回合机制定义 | Demo版可简化为自动结算，但需确认 | P1 |
| F-7 | 缺少 Vue 组件错误边界 | 建议全局 errorHandler，工作量小 | P2 |
| D-7 | 缺少数据调试工具 | 建议开发初期就引入，提升调试效率 | P2 |

---

## 七、总体评价

### 综合评分：8.0 / 10

**优势总结**：
- 架构设计整体成熟，数据驱动理念贯彻到位
- Vue 3 + Pinia 的选型匹配游戏类型，不过度设计
- 存档系统的多槽位策略、校验机制考虑周全
- 事件调度器的权重系统、条件引擎设计专业
- 文件结构清晰，模块职责划分合理

**主要风险**：
- **存储安全**（localStorage容量、解压失败降级）—— 已通过分级存储和降级方案缓解
- **数据一致性**（事务回滚、负值防护）—— 已通过快照回滚和钳制规范缓解
- **可调试性**（随机种子、数据调试工具）—— 已引入 Seeded RNG 解决核心问题
- **版本迁移**（老存档兼容）—— 已补充迁移链实现细节

**开发建议**：
按文档中的 P0 优先级顺序推进，重点关注：
1. 先搭 Seeded RNG + 数据变更事务框架（G-1, G-2），这是一切的基础
2. 尽早实现存档系统并做压力测试（存满→读写→崩溃恢复）
3. 数值计算引擎写单元测试，覆盖极端值场景

---

*评审报告结束*
